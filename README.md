# tg-bot-tester-skill
Claude Code skill for end-to-end testing Telegram bots via browser automation. Interacts through Telegram Web — sends   commands, clicks inline buttons, reads responses — then verifies against data stores and logs. Stack-agnostic: works with any language,   framework, database, and deployment method.
